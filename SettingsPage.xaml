<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="App1.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App1"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <StackPanel>
            <TextBlock Text="Configure Number-Converter" HorizontalAlignment="Center" FontWeight="Bold" Margin="0,10,0,0"/>
        </StackPanel>
        <StackPanel Orientation="Vertical" Margin="110,50,0,0">
            <StackPanel Orientation="Horizontal">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                </Grid>
                <TextBlock Text="Number of Preview-Lines: " />
                <NumberBox Value="5" SpinButtonPlacementMode="Inline" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Add new Filter for: "/>
                <DropDownButton Content="Convertion for">
                    <DropDownButton.Flyout>
                        <MenuFlyout Placement="Bottom">
                            <MenuFlyoutItem Text="SMOPS"/>
                            <MenuFlyoutItem Text="eoLive"/>
                            <MenuFlyoutItem Text="eoSight"/>
                        </MenuFlyout>
                    </DropDownButton.Flyout>
                </DropDownButton>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
